{
  "name": "anglog",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "pnpm --filter @anglog/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
